# 0.6.1 驗證紀錄

日期：2026-09-09（台灣時間）。

## 實際桌面接續：10 輪通過

依使用者要求，在目前的 Codex 聊天視窗測試；不是另行完成的 ChatGPT Chat 模式驗證。

測試指令只用 `printf` 印出 `CTR_AUTO_061_ROUND_01` 到 `CTR_AUTO_061_ROUND_10`，不操作專案檔案。每輪均確認：

- 指令來源紀錄為 `automatic_response`。
- 10 個指令識別碼互不重複，每個只執行一次。
- 每輪結束碼都是 0，實際回傳編號與輪次一致。
- 每輪都有對應的回傳按鍵紀錄，且聊天視窗收到結果。

先前由使用者手按複製完成的測試不計入這 10 輪。

## 模擬與建置

1000 輪自動接續狀態模擬通過，剛好派送 1000 段指令；模擬完成訊息不會觸發第 1001 段。這不是連線 ChatGPT 跑了 1000 輪。

另驗證複製動作回報成功但未更新剪貼簿的情況、滑鼠備援的單次派送，以及備援失敗後停止。macOS／Windows 建置測試通過。

## 限制

- 實際 10 輪是簡單輸出指令，不代表大型專案或真實 1000 輪已驗證。
- 當前測試結束訊息發出後的實際自動停止，尚需後續 log 確認；本紀錄不把模擬停止測試當作實機證據。
- Windows 維持 0.5.0 預覽版，尚未加入 macOS 全自動接續功能。
- 需要 Mac 解鎖並保持目標對話在前景；ChatGPT 介面變動仍可能影響辨識。
